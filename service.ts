import { ServiceDescriptor } from '@selfage/service_descriptor';

export let USER_WEB_SERVICE: ServiceDescriptor = {
  name: "UserWebService",
  path: "/u/w",
}

export let USER_NODE_SERVICE: ServiceDescriptor = {
  name: "UserNodeService",
  path: "/u/n",
}
